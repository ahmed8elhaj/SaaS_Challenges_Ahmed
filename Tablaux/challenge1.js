const element = [10, 20, 30, 40, 50];

function afficherElements(tab[]) {
    tab = element.length;

    for (i = 0; i < tab; i++) {
        console.log(element[i]);
    }
    return
}
afficherElements();
